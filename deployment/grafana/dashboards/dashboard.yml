# Grafana dashboard provisioning

apiVersion: 1

providers:
  - name: 'Materials-Simulato-R'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
